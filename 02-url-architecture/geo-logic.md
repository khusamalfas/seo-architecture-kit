# GEO Logic

Defines how city/region/language are reflected in the structure.

## Options
1. Country subfolders (`/uk/`, `/de/`)
2. Language subfolders (`/en/`, `/de/`)
3. City-level subfolders (`/moscow/`, `/berlin/`)

Choose one model and stick to it.

## Best Practices
- Always pair GEO structure with proper hreflang (if multilingual).
- Avoid creating thin city pages – each must have local value (content, data, offers).
