# Server-Side Tagging (Concept)

- Use a server-side GTM container where possible.
- Send events from backend for critical actions (purchases, bookings).
- Reduce client-side JS load and blocking scripts.
